
-arch turing # indicates 4 Sub-Core per SM
-allocation_policy base # allocation policy (base, dst, etc.)
-eviction_policy lru # replacement policy, (LRU, FIFO, etc.)
-n_entry_per_warp 8 # number of entries per warp

